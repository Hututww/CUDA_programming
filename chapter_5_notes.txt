· 前四章关注程序正确性，第五章开始关注cuda文件的性能（执行速度）
· c++有用到clock（）函数，虽然cs106b和科的课都没有用到（不过在这章第一个cpp版本样例里学习到了）
· CUDA同样也有一种基于CUDA事件的计时方式（然后在示例代码里面学习了计时方式的具体用法）
· 接触到了更多cuda指令：
	cudaEventCreate（&event）初始化并标记，创建事件
	cudaStreamCreate（&stream）：创建一个CUDA流
	cudaEventRecord（event, stream）：在指定的流中记录事件，当流中所有在此调用之前的操作都完成后，这个事件才会被触发
	cudaEventSynchronize（event）：阻塞CPU线程，直到指定的事件被触发
明天继续！
——25.04.09
· 学习了三串代码，了解了程序计时的方法与单双浮点计算用时差别
· 如果把GPU的复制数据时间也计入的话，实际上C++程序的性能更好用时更少
· 接下来学习真正需要用GPU进行加速的计算任务类型与特点
——25.04.10
· 13章里用到的分子动力学程序会在CUDA程序的帮助下有速度上的大幅提高
· 理解了影响CUDA程序性能的三个因素：
	数据传输占整个程序的比例
	算数强度（即算法的复杂程度）
	并行规模（一块GPU上驻留的线程数量）
so在编写程序的时候应该对于这些内容有相关优化的算法
· 学习了CUDA数学函数库的内容（在本书学习中统一用双精度浮点处理变量就行）但是在实际选择函数的过程中需要考虑使用数学函数还是内建函数
第五章结束！
——25.04.11